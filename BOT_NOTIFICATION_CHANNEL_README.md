# 등장곡 봇 알림 채널 지정 기능

이번 수정은 웹사이트 변경이 필요 없습니다.
현재 사용 중인 최신 봇 프로젝트에 아래 두 파일만 덮어쓰세요.

- `main.py`
- `storage.py`

## 명령어

### 현재 채널로 지정
```text
!알림채널설정
```

### 특정 채널로 지정
```text
!알림채널설정 #봇알림
```

별칭:
```text
!출력채널설정 #봇알림
!로그채널설정 #봇알림
```

### 현재 설정 확인
```text
!알림채널
```

### 설정 해제
```text
!알림채널해제
```

설정/해제는 서버 관리자 또는 OWNER_ID만 가능합니다.

## 지정한 채널로 이동하는 메시지

- `🎶 등장곡 플레이어` 현재 곡 카드
- 자동 등장곡 재생 실패/파일 없음 오류
- 통화방이 비어서 자동 퇴장하기 전 안내
- 빈 통화방 자동 퇴장 완료 안내

일반 명령어의 짧은 성공/실패 응답은 명령어를 입력한 채널에 그대로 표시됩니다.

## 저장

채널 ID는 기존 `guilds/{서버ID}` 설정에 같이 저장되므로
Firebase를 사용 중이면 Railway 재시작/재배포 후에도 유지됩니다.

## 필요한 Discord 권한

지정하려는 텍스트 채널에서 봇에게 아래 권한이 필요합니다.

- 채널 보기
- 메시지 보내기
- 링크 임베드

## 적용

1. ZIP 압축 해제
2. 기존 GitHub 봇 프로젝트의 `main.py`, `storage.py`를 덮어쓰기
3. GitHub에 반영
4. Railway 재배포
5. Discord에서 `!알림채널설정 #원하는채널` 실행

Firebase 웹사이트는 다시 deploy할 필요 없습니다.
