# 검증 결과 / 남은 현장 확인

작성: 2026-09-19

## 완료한 검증

| 구분 | 결과 | 범위 |
|---|---|---|
| Python 구문 검사 | 통과 | `python -m compileall -q .` |
| JavaScript 구문 검사 | 통과 | `node --check static/app.js` |
| 자동 테스트 | **45개 통과** | 아래 분류 참고 |
| 화면 렌더링 검사 | 통과 | Chromium, 너비 360/390/768/1440px에서 가로 넘침 없음 |
| 화면 스크립트 | 오류 0건 | 오프라인 API 응답 fixture로 검색·타순·효과음·진단·모바일 로그아웃 표시 확인 |
| 기존 sounds 보존 | 52개 파일 해시 일치 | 원본과 SHA-256 비교; 플레이스홀더 파일 포함 |
| 기본 오디오 유효성 | 36/36개 확인 | 실제 ffprobe로 길이와 파싱 검사 |
| FFmpeg PCM 변환 | 통과 | 기본 오디오 1개를 1초 분량으로 디코딩; Discord 전송 아님 |

자동 테스트는 입력 검증 9개, SQLite 저장 동작 9개, 실제 aiohttp HTTP API 검사 15개, 모의 음성 연결 검사 12개입니다.
로그인/로그아웃, 세션 쿠키, CSRF, 교차 출처 차단, 시도 제한, 파일 검사/업로드, 실제 WAV 처리, 바이트 범위 응답, 곡/타순 저장과 삭제, 분할 JSON 요청, 크기 제한 등을 확인했습니다.
음성 연결 테스트는 실제 Discord 대신 모의 객체를 사용해 연결 재사용·이동·권한·인원 제한·실패 정리·재시도·동시 연결 직렬화 등을 확인한 것입니다.

브라우저의 일반 URL 접속은 제작 환경 정책으로 제한되어, 화면은 로컬 HTML/CSS/JavaScript와 명시적인 오프라인 API 응답으로 렌더링했습니다.
따라서 화면 확인 결과를 브라우저→실제 서버 로그인/업로드의 완전한 종단간 검증으로 보아서는 안 됩니다.
서버 HTTP API는 이와 별도로 aiohttp 테스트 서버에서 실제 실행했습니다.
미리보기의 선수 데이터는 화면 확인용 예시이며, 배포 ZIP에는 이 예시 DB나 로그인 세션이 포함되지 않습니다.

## 제작 환경에서 하지 못한 검증

- 실제 봇 토큰으로 Discord 로그인, 실제 음성 채널 접속/이동, 암호화된 오디오 송출 및 청취.
- 실제 Firebase 서비스 계정으로 기존 곡/타순 데이터 읽기·쓰기.
- 실제 YouTube 영상으로 yt-dlp 주소 추출 및 스트리밍 재생.
- requirements 전체 설치 및 Docker 이미지 빌드. 제작 환경의 패키지 다운로드 네트워크가 차단되어 설치 시도는 실패했습니다.
- 사용자 호스팅의 UDP 송수신, 방화벽/NAT, HTTPS 프록시, 볼륨 마운트.

`discord.py`, `yt-dlp`, `firebase-admin`은 이 테스트 환경에 설치되어 있지 않습니다. 검사 결과를 이 패키지들과의 실제 통합 실행 성공으로 해석하면 안 됩니다.
Dockerfile은 빌드 과정에서 discord/PyNaCl/davey/yt-dlp import, Deno 및 FFmpeg 존재를 확인하도록 구성했습니다. 이는 사용자 환경에서 빌드할 때 수행됩니다.

## 실제 서버 적용 후 확인 순서

1. 기존 봇 프로세스를 종료하고 전체 프로젝트와 새 requirements로 설치/빌드합니다.
2. 기존 Firebase 설정을 유지하고 WEB_ADMIN_PASSWORD를 추가합니다. Message Content Intent도 확인합니다.
3. 일반 통화방에 들어간 상태에서 `!진단`, `!입장`으로 노래 없이 음성 연결부터 시험합니다.
4. 기본 효과음 또는 본인 소유의 MP3를 올려 재생합니다. 연결됨 표시와 실제 소리 여부를 각각 확인합니다.
5. YouTube 곡을 재생해 별도로 확인합니다. YouTube 실패만으로 음성 접속 실패라고 판단하지 않습니다.
6. 로그인한 웹에서 곡 등록→타순 저장→현재 경기 팀 선택→Discord 재생을 확인합니다.
7. `/data` 볼륨을 확인한 후 재시작해 곡, 타순, 업로드 오디오가 유지되는지 확인합니다.

## 수정한 주요 위험 지점

원본 requirements의 discord.py 2.4.0은 새로운 DAVE 지원 전 버전입니다. 공식 변경 이력의 DAVE 지원 버전으로 변경했습니다.
원본은 곡을 재생할 때 기존 음성 연결을 끊고 다시 연결했습니다. 이를 재사용/이동 우선과 서버별 잠금으로 바꿨습니다.
동기 Firebase/yt-dlp 작업을 별도 스레드로 이동해 이벤트 루프와 음성 heartbeat를 막지 않도록 했습니다.
`nixpacks.toml`의 존재하지 않는 `bot.py` 시작 명령을 `main.py`로 고쳤습니다.
사용자 서버의 실패 로그가 없으므로, 위 문제 중 무엇이 실제 장애의 직접 원인이었는지는 확정하지 않았습니다.

## 파일 범위

배포 ZIP에는 실행 소스, 기본 효과음, 정적 웹 파일, 배포 설정, 환경변수 예시와 자동 테스트가 포함됩니다.
실제 토큰/서비스 계정/쿠키, `.env`, 테스트용 DB, 업로드 데이터, 가상환경, Python 캐시는 넣지 않습니다.
