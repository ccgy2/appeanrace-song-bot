# 곡 자체 볼륨 패치 — 테스트 보고서

검사 버전: `song-volume-20260926-1` · 2026-09-26
기준 소스: `APPEARANCE_20260926_FULL.zip`의 `studio-20260926-1`.

## 완료한 검사

### Python 회귀·API·저장·재생 로직

실행: `python -m unittest discover -s tests -v`
결과: **181개 발견, 173개 통과, 8개 건너뜀, 실패/오류 0**. 실행 시간 15.421초.

신규 `tests/test_song_volume.py` **38개 전부 통과**:

- 정수 0~200%, 기본 100%, 숫자 문자열, 0%를 기본값으로 잘못 대체하지 않음.
- 음수·201·소수·공백·bool·null·NaN·무한대 등 부적합 입력 거부.
- 등장곡/응원가/상황별 노래별 저장, 팀·종류 구분, 기존 필드 누락 기본값, 구버전 쓰기에서 저장된 볼륨 보존.
- 새 Store 인스턴스로 재조회한 값, 이름 변경·Discord 사용자 ID·타순·경기 연결 보존, 설정 내보내기.
- 관리자/등록삭제자 수정 허용, 재생자/유저 수정 거부, CSRF 검사 유지.
- 실 로컬 aiohttp HTTP 요청과 임시 SQLite DB의 등록·수정·상태 캐시 갱신.
- 직접 업로드한 경기 사운드의 개별 볼륨, 구버전 요청과 기본 효과음/기존 파일명 상황 유지, 순서 변경 시 볼륨 보존.
- 라이브러리 연결 곡에 경기 상황 볼륨을 중복 적용하지 않음.
- 곡×봇 전체 배율, 자동 ID 선택과 수동/5초 재생, 다음 곡으로 볼륨 누수 없음, 0% 무음과 master 0%.
- 재생 중 master 변경 시 곡 배율을 유지하고 재생을 다시 시작하지 않음. 다른 팀의 master 설정을 현재 소스에 적용하지 않음.
- 저장한 곡 볼륨은 다음 재생에서 읽음. 볼륨만 바꿔서는 상황별 순서 재생 진행 위치를 초기화하지 않음. 두 Player 인스턴스의 소스 상태 구분.
- 테스트 WAV 업로드/볼륨 변경/백업에서 원본 파일 바이트·원본 이름 유지, 설정 백업의 볼륨 메타데이터 확인.

Discord 연결/오디오 소스와 YouTube 추출은 모의 객체입니다. 실제 Discord에 신호를 보내거나 청취한 검사가 아닙니다. 업로드 검사에는 설치된 실제 FFprobe를 사용했습니다. 일부 실패 경로를 고의로 만드는 테스트 로그의 timeout traceback은 예상 경로이며, unittest 최종 실패/오류는 0입니다.

기존 `tests/test_linked.py`는 새 빌드 번호 기대값만 갱신했습니다. 건너뛴 8개는 기준 ZIP부터 누락된 구형 `setup_deploy.py` 대상 검사로 이번 볼륨 기능의 실패를 숨기기 위해 제외한 것이 아닙니다.

### Chromium 브라우저 UI

실행: `python tests/browser_song_volume.py` → **18개 시나리오 통과**.
실행: `python tests/browser_studio_update.py` → **17개 기존 시나리오 통과**.
합계 **35개 통과, JavaScript 페이지 오류 0**.

실제 Chromium의 DOM/키보드/숫자 입력/슬라이더 포인터 드래그를 실행했고, API·업로드·설치 이벤트는 명시적 테스트 fixture입니다. localhost 실제 서비스 워커 통합이나 운영 웹사이트 접속 테스트가 아닙니다.

신규 18개는 다음을 확인했습니다.

1. 등록삭제자의 독립된 곡 볼륨 위젯과 목록 배지.
2. 곡 수정 시 저장된 볼륨·ID·소스를 불러옴.
3. 슬라이더↔숫자 동기화, 0%, 100% 초기화, 증폭 경고.
4. 빈칸·소수·음수·201%의 저장 차단.
5. 곡 저장 요청에는 볼륨을 넣지만 master/원본/재생 요청은 변경하지 않음.
6. 세 종류 라이브러리와 구버전 기본 100%.
7. 구버전 서버에 새 볼륨을 조용히 누락 저장하지 않음.
8. 곡 편집 화면의 320·360·390·768·1500px 가로 넘침 없음.
9. 기본 효과음 0% 저장 후 다시 열기.
10. 라이브러리 저장 볼륨 표시와 직접 파일별 편집 구분.
11. 곡 순서 변경 때 볼륨도 해당 파일과 이동.
12. 실제 range 드래그가 파일 순서 드래그로 오인되지 않음.
13. 경기 사운드 긴 파일명/볼륨 입력의 동일 5개 폭 레이아웃 검사.
14. 잘못된 경기 파일 볼륨 저장 차단, 라이브러리 중복 배율 미전송.
15. 기존 지정 파일 상황에서 볼륨만 바꿔도 파일명 보존.
16. 재생자·유저에게 볼륨 수정 UI 없음.
17. 현재 재생 정보의 곡/봇 전체 값 구분.
18. 로그아웃 시 편집 상태와 볼륨 입력 초기화.

기존 17개 회귀는 역할별 화면, 자동 ON/OFF, 모든 종류 다중 연결, 파일 선택/드롭/순서 변경, 기본 상황 삭제/복원, 명시적 저장, 세션 만료, 설치 안내를 확인했습니다. 기존 브라우저 fixture에 새 `songVolumeEnabled`와 기능 목록만 추가했습니다.

브라우저 검사 코드는 Playwright/Chromium 개발 환경용이며 운영 서버 추가 의존성은 아닙니다. 이번 환경은 Linux의 `/usr/bin/chromium`을 사용했습니다.

### 구문·정적 파일·데이터 보존

- `python -m compileall -q .` 통과.
- root/static의 `app.js`, `sw.js`, `pwa.js` Node 구문 검사 통과.
- 웹사이트/static의 `app.js`, `sw.js` Node 구문 검사 통과.
- `웹사이트/`의 `node scripts/check-hosting.cjs` 통과. 이는 제공본에 남은 별도 웹 사본 검사이며 현재 사용자의 Railway 배포에 Firebase를 요구하지 않습니다.
- 변경한 UI 4개 파일은 root/static과 웹사이트/static에서 바이트가 동일합니다.
- 이전 전체 ZIP의 **기본 음원 52개를 바이트 비교해 모두 동일**함을 확인했습니다.
- 기존 `requirements.txt`, `Dockerfile`, `config.py`, `main.py`, `assets.py`, `backups.py`, `permissions.py`, 양쪽 `config.js`는 바이트가 동일합니다.

## 확인하지 못한 항목

실제 Discord 토큰을 사용한 봇 부팅·음성 전송·통화방/스테이지 청취, 실제 YouTube 영상 추출, 실물 삼성폰/아이폰/PWA 갱신, Railway 배포, 운영 Volume/Firestore는 테스트하지 않았습니다. 운영 데이터에 접근하거나 쓰지 않았습니다.

이 작업 환경에서는 discord.py/yt-dlp의 지정 버전을 패키지 인덱스에서 설치할 수 없어서 전체 운영 의존성을 갖춘 봇 런타임은 실행하지 않았습니다. 버전이 존재하지 않는다고 단정하는 것이 아니라 이 환경의 설치 제한입니다. 운영 의존성과 Dockerfile은 변경하지 않았습니다. 따라서 재생 검사는 실제 Player 코드에 모의 Discord/YouTube 객체를 주입한 범위입니다.

## 배포 후 청취 확인

작은 master 볼륨에서 같은 곡을 100% → 50% → 150%로 각각 저장·재생하고, 다른 곡의 저장 값이 유지되는지 확인하세요. 재생 중 master를 바꾼 뒤에도 곡 배율이 유지되는지, 자동 ID 등장곡과 직접 업로드 경기 사운드에서도 설정이 적용되는지 확인하세요. 증폭에서 찌그러짐이 생기면 곡 또는 master 값을 낮추세요. 새 설정의 비교는 내 기기 원본 미리듣기가 아니라 Discord에서 합니다.

## 패치 패키징 확인

이전 전체 ZIP에 봇 패치와 웹사이트 패치를 실제로 덮어쓴 별도 디렉터리를 만들고 다시 검사했습니다. 이 덮어쓰기 결과에서도 181개 중 173개 통과·8개 건너뜀·실패/오류 0이었습니다(16.069초).

봇 패치 9개, 웹사이트 패치 10개 파일입니다. 기존 파일 15개 변경과 신규 테스트/안내 4개만 포함하고, 기준 소스와 같은 파일은 넣지 않았습니다. 두 패치 사이에 중복 덮어쓰기 경로는 없으며, 그 외 기준 파일이 바뀌지 않는 것도 바이트 비교로 확인했습니다. 패치는 둘 다 기존 저장소 루트를 기준으로 풀도록 만들었습니다.
