# APPEARANCE — 곡 자체 볼륨 패치 적용 안내

버전: `song-volume-20260926-1` · 기준 소스: 이전에 전달한 `APPEARANCE_20260926_FULL.zip` (`studio-20260926-1`)

이번 전달물은 전체 프로젝트가 아니라 **봇 변경 파일 ZIP + 웹사이트 변경 파일 ZIP**입니다. 아직 사용자의 GitHub/Railway에 배포된 상태는 아닙니다. 기준 소스 이후에 사용자가 별도로 같은 파일을 수정했다면 덮어쓰기 전에 그 변경사항을 비교·병합하세요.

## 1. 무엇이 달라졌나요?

등장곡·응원가·상황별 노래의 등록/수정 화면에 **곡 자체 볼륨** 슬라이더, 숫자 입력, **100%로** 초기화 버튼을 넣었습니다. 범위는 0~200%이며 기존 데이터에 설정이 없으면 100%로 재생합니다. 0%는 무음이고 100%는 추가 증폭/감쇠 없는 상태입니다. 200%는 신호 배율 2배이지 사람이 느끼는 소리가 정확히 2배라는 뜻은 아닙니다.

곡 볼륨은 그 팀·라이브러리 종류·곡에 저장됩니다. 다른 팀이나 다른 종류에 같은 이름으로 등록한 곡은 별도입니다. YouTube 링크와 업로드 파일 모두 같은 설정을 사용합니다.

출력 신호 배율은 `봇 전체 볼륨 × 곡 자체 볼륨`입니다. 예를 들어 봇 전체 50%, 곡 150%라면 원본 대비 0.75배 신호로 출력합니다. 곡 볼륨을 저장해도 봇 전체 볼륨은 바뀌지 않습니다. 나중에 봇 전체 볼륨을 바꿔도 현재 곡의 배율은 유지합니다.

원본 파일을 재인코딩하거나 덮어쓰지 않습니다. 볼륨 설정은 메타데이터로 보존하며, 기존 원본 다운로드와 오디오 포함 ZIP 백업의 파일 바이트는 그대로입니다. 곡·경기 상황의 볼륨 메타데이터도 설정 백업에 포함됩니다.

**100%를 넘기면 큰 부분에서 소리가 찌그러질 수 있습니다.** 조금씩 올려 실제 통화방에서 확인하세요. 음질 개선·자동 음량 평준화 기능이 아닙니다.

## 2. 사용 방법과 적용 시점

음악 라이브러리 → 등장곡/응원가/상황별 노래 선택 → 해당 곡 **수정** → **곡 자체 볼륨** 조절 → **변경 내용 저장**.

새 곡을 등록할 때도 같은 항목을 설정할 수 있습니다. 목록에는 `곡 80%`처럼 저장된 값이 표시되고, 현재 재생 정보에는 `곡 145% × 봇 50%`처럼 두 값을 구분합니다. 하단의 기존 슬라이더는 이름을 **봇 전체**로 바꿨습니다.

**곡 볼륨은 저장 후 새로 요청한 Discord 재생부터 적용됩니다.** 이미 재생/준비 중인 음악을 강제로 끊거나 파일을 변환하지 않습니다. 곧바로 확인하려면 저장 후 재생 버튼을 다시 누르세요. 자동 등장곡, 타순/다음 타자, 수동 재생, Discord 통화방 5초 미리듣기도 같은 곡 설정을 사용합니다.

브라우저의 **내 기기 미리듣기**와 등록 전 오디오 미리듣기, 원본 다운로드는 원래 음량입니다. 설정한 소리를 확인할 때는 Discord 통화방 재생을 사용하세요.

볼륨 수정 권한은 **관리자·등장곡 등록/삭제자**입니다. 등장곡 재생자는 저장된 값으로 재생할 수 있지만 곡 볼륨을 저장할 수 없고, 유저는 기존 조회 권한을 유지합니다. 서버 API에서도 권한과 0~200 정수 범위를 검사합니다.

## 3. 경기 사운드 볼륨

경기 사운드의 **곡 · 순서 편집**에서 확인하세요.

- **라이브러리 연결 곡:** 해당 라이브러리의 곡 볼륨을 그대로 사용합니다. `곡 N% · 음악 라이브러리의 ‘수정’에서 변경`을 표시합니다. 경기 상황에서 다시 한 번 곱하지 않습니다.
- **경기 사운드에 직접 업로드한 파일:** 연결 목록에서 파일마다 **이 파일 볼륨**을 설정합니다. 같은 파일을 다른 경기 상황에 연결한 경우에는 각 연결에 저장된 값이 적용됩니다. 드래그/화살표로 순서를 바꿔도 파일과 볼륨이 같이 이동합니다.
- **별도 연결 없이 사용하는 기본 효과음:** **기본 효과음 볼륨**으로 그 상황의 기본 소리 크기를 설정합니다. 그 상황에서 무작위로 뽑는 기본 파일들에는 공통으로 적용됩니다. 기본 제공 파일 하나하나를 별도 편집하는 기능은 아닙니다.

마지막에 **변경사항 저장**을 누르세요. 값만 바꿔 닫으면 저장되지 않습니다. 곡 순서 자체를 바꾸지 않고 볼륨만 바꾸면 순서 재생의 진행 위치를 초기화하지 않습니다. 구버전 파일명 지정 상황도 볼륨만 저장할 때 원래 지정 파일을 유지합니다.

## 4. 현재 구성: GitHub → Railway에 적용

현재 사용 중인 Railway 주소에서 봇·API·웹 화면을 함께 제공하는 구성에 맞췄습니다. **두 ZIP 모두 기존 저장소의 루트, 즉 `main.py`가 있는 위치를 기준으로 반영합니다.** 별도로 Firebase를 배포할 필요가 없습니다.

### GitHub를 브라우저에서 사용하는 경우

1. 두 ZIP을 각각 풀고, 안의 파일/폴더를 임시 폴더 하나에 합치세요. `tests` 폴더는 합치고 다른 기존 파일은 지우지 마세요. ZIP 자체나 ZIP 이름의 바깥 폴더를 GitHub에 올리는 것이 아닙니다.
2. Railway가 연결된 기존 GitHub 저장소와 배포 브랜치를 엽니다. 저장소 루트에서 **Add file → Upload files**를 누르고, 합친 폴더 안의 파일과 폴더를 끌어 넣습니다. 업로드 경로가 `player.py`, `webapp.py`, `static/app.js` 등 기존 경로와 같은지 확인하세요.
3. 변경 내용을 커밋합니다. 보호 브랜치라면 새 브랜치/PR을 거쳐 Railway 연결 브랜치에 병합하세요. 가능한 한 두 패치를 같은 커밋으로 반영해 구버전 서버·신버전 화면이 섞이는 시간을 줄이세요.
4. 자동 배포가 켜져 있으면 연결 브랜치의 새 커밋으로 Railway 배포가 시작됩니다. 배포 완료와 실패 로그를 확인합니다. 자동 배포가 꺼져 있거나 watch path 때문에 건너뛰면 Railway의 **Deploy Latest Commit**으로 최신 커밋을 배포하세요. 오래된 배포를 재실행하는 것과 구분하세요.
5. 기존 Railway 사이트를 새로고침합니다. 설치한 웹앱은 새 버전 안내를 적용하거나 닫았다가 다시 열어 확인하세요. 볼륨 항목이 없으면 PC에서는 강력 새로고침도 확인합니다. 실제 모바일/PWA 업데이트는 운영 환경에서 확인이 필요합니다.

**중요:** 웹사이트 ZIP의 루트 `static/`가 Railway 화면용입니다. `웹사이트/static/`만 바꾸면 Railway 화면이 바뀌지 않습니다. ZIP에는 별도 웹사이트 사본을 유지하기 위한 `웹사이트/static/`의 같은 변경 파일도 포함돼 있지만, 기존 두 `config.js`는 서로 다르므로 교체하지 않습니다.

루트 `static/app.js`가 아니라 `static/static/app.js` 또는 `APPEARANCE_WEBSITE_VOLUME_PATCH/static/app.js`처럼 중첩되면 잘못 적용한 것입니다.

연결 검사 빌드: `song-volume-20260926-1`. 새 화면은 서버가 곡 볼륨을 지원하지 않으면 안내를 표시하고 곡·경기 사운드 저장을 중단해 볼륨 값이 조용히 누락되는 것을 막습니다.

### 데이터/설정 보존

기존 Railway Volume과 `DATA_DIR`, 기존 계정/곡 DB, 업로드 파일을 유지하세요. `.env`, 토큰, 비밀번호, DB, `uploads/`, `sounds/`를 삭제하거나 초기화하지 마세요. 새 환경변수·새 서비스·새 Volume·새 의존성 설치는 필요하지 않습니다. 패치에는 사용자 운영 데이터가 들어 있지 않습니다. 적용 전 기존 설정·음악 백업을 권장합니다.

## 5. 변경 파일 목록

### 봇 패치: APPEARANCE_BOT_VOLUME_PATCH.zip

실행 파일 5개: `validation.py`, `storage.py`, `event_tracks.py`, `player.py`, `webapp.py`.
검사 파일 2개: `tests/test_song_volume.py`(신규), `tests/test_linked.py`(버전 기대값 갱신).
안내 2개: `SONG_VOLUME_UPDATE.md`, `TEST_REPORT_SONG_VOLUME.md`.

### 웹사이트 패치: APPEARANCE_WEBSITE_VOLUME_PATCH.zip

화면 파일 8개: `static/index.html`, `static/app.js`, `static/style.css`, `static/sw.js` 및 `웹사이트/static/`의 같은 4개 파일.
브라우저 검사 파일 2개: `tests/browser_song_volume.py`(신규), `tests/browser_studio_update.py`(기능 플래그 fixture 갱신).

이전 전체 ZIP과 바이트가 같은 파일은 포함하지 않습니다. 삭제해야 하는 파일은 없습니다. `requirements.txt`, `Dockerfile`, `main.py`, 기존 연결 설정, 기본 음원 52개는 변경하지 않았습니다.

## 6. 테스트 범위

자세한 결과와 운영 환경에서 남은 확인은 `TEST_REPORT_SONG_VOLUME.md`에 구분했습니다. 로컬 테스트 통과가 실제 Discord 음성 청취나 Railway 운영 배포 성공을 뜻하지는 않습니다.

공식 배포 절차 참고(2026-09-26 확인):
- GitHub 파일 업로드: https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository
- Railway GitHub 자동 배포: https://docs.railway.com/deployments/github-autodeploys
- discord.py의 PCM 볼륨 변환 구현: https://github.com/Rapptz/discord.py/blob/master/discord/player.py
