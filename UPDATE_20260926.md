# APPEARANCE — 2026-09-26 업데이트 적용 안내

버전: `studio-20260926-1`

**봇 서버 코드와 웹사이트를 모두 업데이트해야 합니다.** 이 ZIP은 수정된 소스입니다. 사용 중인 Railway/Firebase 계정에 자동 배포된 상태는 아닙니다. 앱은 같은 웹사이트를 홈 화면에 설치하는 PWA이며, 별도 Android APK / iOS IPA나 앱스토어 배포본은 아닙니다.

## 1. 먼저 데이터 보존

현재 웹의 연결 진단에서 오디오 포함 음악 ZIP 백업과 곡·타순 JSON 백업을 받으세요. 기존 코드로 받은 백업의 UUID 파일명은 수정본 적용 후 새로 받는 백업에서 원본명으로 바뀝니다.

기존 Railway Volume과 `DATA_DIR`를 유지하세요. `songs.sqlite3`, `songs.sqlite3-wal`, `songs.sqlite3-shm`, `uploads/`, 비밀 `.env` 파일을 삭제하거나 새 빈 데이터로 덮어쓰지 마세요. SQLite를 직접 복사할 때는 봇을 중지하거나 일관된 볼륨 스냅샷을 사용하세요. 이 수정본에는 사용자의 실제 서버 DB·업로드 파일·계정 데이터가 들어 있지 않습니다.

기존 `DISCORD_TOKEN`, 보조 봇 토큰, `WEB_ADMIN_PASSWORD`, 저장소 관련 환경변수와 배포 프로필을 유지하세요. 이 업데이트는 DB 초기화, 계정 재생성, 비밀번호 재설정, SQLite↔Firestore 전환을 수행하지 않습니다. 기존 제공본의 기본 저장소 선택도 바꾸지 않았습니다.

## 2. 봇 서버에 적용

전체 ZIP을 별도 폴더에 푼 뒤, 그 안의 파일들을 기존 봇 프로젝트 루트(`main.py`가 있는 곳)에 반영하세요. 새로운 `permissions.py`, `event_tracks.py`를 빼먹으면 안 됩니다. `static/`의 앱 설치 파일과 아이콘도 함께 반영하세요.

GitHub 연동 Railway를 사용 중이면 해당 저장소에 커밋·푸시하고 배포 결과를 확인하세요. 새로운 서비스/새 Volume을 만들 필요는 없습니다. 기존 한 서비스/한 replica의 청팀·백팀 두 봇 구조를 유지합니다.

주요 서버 수정 파일은 `main.py`, `voice.py`, `player.py`, `webapp.py`, `storage.py`, `backups.py`, 새 `permissions.py`, `event_tracks.py`입니다. `requirements.txt`와 Dockerfile은 원본 그대로입니다.

Python 테스트는 다음 명령으로 실행할 수 있습니다. 브라우저 테스트에는 별도의 Playwright/Chromium 개발 환경이 필요하며 운영 서버 의존성은 아닙니다.

```sh
python -m unittest discover -s tests -v
python -m compileall -q .
node --check static/app.js
node --check static/pwa.js
node --check static/sw.js
```

Discord 실제 연결을 시작하려면 기존 환경변수/의존성이 갖춰진 운영 환경에서 기존과 같이 `python main.py`로 실행합니다. 이번 작업 환경의 테스트 범위는 `TEST_REPORT_STUDIO.md`를 확인하세요.

## 3. Firebase 웹사이트에 적용

전체 ZIP 안의 `웹사이트/` 폴더가 Firebase용입니다. 그 폴더에서 아래 명령을 실행하세요. 별도 웹사이트 ZIP을 받은 경우에는 기존 `웹사이트/` 안에 풀고 같은 명령을 실행합니다. 두 ZIP을 중복 적용할 필요는 없습니다.

```sh
cd 웹사이트
node scripts/check-hosting.cjs
firebase deploy --only hosting
```

`firebase.json`도 수정했으므로 HTML/JS만 복사하지 마세요. 서비스 워커·웹 앱 manifest용 CSP와 헤더가 추가됐습니다. `.firebaserc`와 연결 대상 URL은 원본 설정을 유지했습니다.

루트 `static/config.js`는 Railway에서 직접 열 때 같은 서버 API를 사용하도록 비어 있고, `웹사이트/static/config.js`는 기존 Railway API 주소를 가리킵니다. 두 파일을 서로 바꾸지 마세요. 이 둘을 제외한 핵심 화면·앱 설치 파일은 동일합니다.

배포 후 웹에 표시되는 연결 상태를 확인하고 새로고침한 뒤 로그인하세요. 최신 웹은 서버의 새 기능 목록도 검사하므로 서버가 구버전이면 업데이트 필요 메시지가 나옵니다.

## 4. 전체 자동 등장곡 ON/OFF

로그인 후 위쪽의 **음성방 입장 시 자동 등장곡** 스위치입니다. 관리자·등록/삭제자·재생자가 변경할 수 있습니다.

설정은 모든 서버·팀·청팀/백팀 봇에 공통 적용되고 DB에 저장됩니다. 기존 데이터에 이 설정이 없으면 기존 동작을 유지하도록 기본 ON입니다. OFF에서는 새 입장/이동에 따른 자동 재생만 차단합니다. OFF로 바꾸는 동안 준비 중인 자동 등장곡도 재생 직전에 다시 검사하며, OFF 후 곧바로 ON으로 바꿔도 이전 요청이 뒤늦게 재생되지 않습니다.

**이미 재생 중인 음악은 OFF로 끊지 않습니다.** 멈추려면 정지를 누르세요. 수동 재생은 OFF에서도 가능합니다. 다른 기기의 화면에는 실시간 상태 갱신 주기에 따라 변경이 표시됩니다.

Discord 명령:

```text
!자동등장곡          현재 설정 확인
!자동등장곡 on       전체 ON
!자동등장곡 off      전체 OFF
```

보조 봇은 기존 `!2` 접두사를 사용합니다. 등록한 Discord 사용자 ID로 곡을 찾는 방식은 유지합니다. 웹의 유저 권한은 수동 조작 권한이며, ON인 자동 ID 등장곡의 대상 자체를 제한하지 않습니다.

## 5. 계정과 Discord 역할

| 역할 | 허용 기능 |
|---|---|
| 관리자 | 모든 음악·재생·경기 사운드·팀·타순·회원·전체 백업 |
| 등장곡 등록/삭제자 | 세 종류 음악 등록·수정·삭제·업로드, 경기 사운드 생성·편집·삭제·복원, 수동 재생, 자동 등장곡 ON/OFF |
| 등장곡 재생자 | 수동 재생·정지·음량·음성방 제어, 자동 등장곡 ON/OFF, 목록 조회 |
| 유저 | 목록 조회, 업로드 원본 다운로드·자기 기기에서 듣기. Discord 재생/설정 변경 불가 |
| 승인 대기 | 관리자 승인 전 로그인 불가 |

웹 **회원 · 권한 관리**에서 역할을 선택하고 **권한 저장**을 누르세요. 과거 `player` 계정을 자동으로 등록/삭제자로 승격시키지 않습니다. 기존에 등록 업무를 하던 사람은 관리자가 `등장곡 등록/삭제자`로 바꿔주세요. 권한을 바꾸면 그 계정의 기존 세션을 무효화하므로 다시 로그인해야 합니다.

웹 계정 권한과 Discord 서버 역할은 서로 별도입니다. Discord에서는 같은 이름의 역할을 부여하거나 관리자 명령을 사용합니다.

```text
!권한설정 @회원 등록삭제자
!권한설정 @회원 재생자
!권한설정 @회원 유저
```

이 명령으로 역할을 부여하려면 봇의 역할 관리 권한 및 역할 서열이 충분해야 합니다. 다른 용도로 부여한 서버 역할은 지우지 않습니다. 기존 Discord `등장곡 재생인` 역할도 재생자로 계속 인식합니다. 웹 로그인은 Discord OAuth로 바꾸지 않았고 기존 아이디·비밀번호 방식입니다.

등록/삭제자 이상에게는 `!삭제 이름`, `!응원가삭제 이름`, `!상황곡삭제 이름`도 제공됩니다. 팀·타순·회원 운영 권한은 관리자에게 남겼습니다.

## 6. 경기 사운드 여러 곡 편집

경기 사운드 카드의 **곡 · 순서 편집**을 누르세요. 등장곡·응원가·상황별 노래를 종류/이름으로 검색하고 여러 개를 체크한 뒤 **선택한 곡 한 번에 추가**를 누릅니다. 같은 이름이어도 종류가 다르면 서로 다른 곡입니다.

여러 파일을 선택하거나 업로드 영역으로 파일들을 드래그하면 순차 업로드하며 성공/실패 수를 표시합니다. 업로드는 한 파일씩 처리해 기존 서버의 용량 검사를 유지하고, 한 파일이 실패해도 나머지 결과를 확인할 수 있습니다. 형식/개수 검사는 업로드 전에도 수행합니다.

연결 목록에서 드래그하거나 ↑/↓로 순서를 바꿀 수 있습니다. 터치 기기에서는 화살표를 사용하세요. **변경사항 저장**을 눌러야 연결과 순서가 적용됩니다. 저장 전 닫으면 연결 편집은 버려지지만 이미 업로드된 원본 파일은 서버에 남습니다.

한 상황당 최대 100곡입니다. 단곡은 첫 곡, 랜덤은 매번 임의의 한 곡, **순서대로 1곡씩**은 버튼을 누를 때마다 다음 곡을 재생합니다. 한 번 눌러 전체 목록을 끝까지 자동 연속 재생하는 모드는 아닙니다. 곡 목록/순서를 바꾸면 해당 상황의 순서 재생은 첫 곡부터 다시 시작합니다.

이전의 상황별 노래 전용 연결은 `situation`으로 읽어 호환성을 유지합니다. 음악 닉네임을 바꾸거나 삭제하면 종류까지 구분해서 연결도 갱신합니다.

## 7. 기본 경기 상황도 삭제·복원

기본 홈런·삼진·볼넷 등과 직접 만든 상황에 모두 **상황 삭제**가 있습니다. 삭제하면 현재 팀 목록에서 빠지고 해당 상황의 재생도 거부합니다. **삭제한 경기 상황 복원**에서 다시 활성화할 수 있습니다.

삭제는 현재 팀의 상황 설정을 비활성화하는 방식입니다. `sounds/` 원본이나 다른 팀의 상황을 지우지 않습니다. 곡 목록의 ‘빼기’는 그 상황에서의 연결만 제거하며 라이브러리 원본은 삭제하지 않습니다.

## 8. 스테이지 채널

웹의 채널 목록에 스테이지도 표시합니다. 연결한 봇이 청중이면 소리를 보내기 전에 발언자 상태를 확인합니다.

채널 보기·연결 권한을 허용하세요. 자동 발언자 전환에는 Discord의 **멤버 음소거(Mute Members)** 권한이 필요합니다. 이 권한이 있으면 `suppress=False`로 전환하고 음성 상태 갱신까지 확인합니다. 권한이 없으면 발언 요청 권한이 있을 때 요청을 보내고, 연결은 유지하면서 관리자에게 발언자로 전환하라는 메시지를 표시합니다. Discord 관리자가 전환한 뒤 다시 연결/재생을 누르세요.

코드가 Discord 권한을 우회하거나 청중 상태에서도 소리가 나온다고 가정하지 않습니다. 서버 음소거가 켜져 있으면 별도로 해제해야 합니다. 실제 서버 권한과 실제 청취 결과는 배포 후 확인해야 합니다.

공식 참고: https://docs.discord.com/developers/resources/voice#modify-current-user-voice-state

## 9. 원본 파일명 백업

오디오 ZIP의 `uploads/`에 등록 당시 메타데이터의 파일명으로 저장합니다. 서버 내부 UUID 저장 방식은 충돌 방지를 위해 유지합니다.

```text
uploads/김선수 등장곡.mp3
uploads/김선수 등장곡 (2).mp3
uploads/우리 팀 응원가.wav
asset-metadata/<파일ID>.json
metadata.json
backup-info.json
백업_안내.txt
```

동일 이름은 `(2)`, `(3)`으로 구분하고, 경로 구분자/운영체제 금지 문자 등은 안전한 문자로 처리합니다. 오디오 바이트는 재인코딩하지 않습니다. 원본명 정보가 과거에 사라졌거나 메타데이터 자체에 다른 이름이 저장되어 있다면 알 수 없는 이름을 추측해 복원하지 않습니다.

`backup-info.json`에 ID↔ZIP 경로↔서버 파일명 매핑과 누락 파일 목록을 저장합니다. 이미 서버에서 없어진 오디오와 YouTube 링크의 실제 오디오, 비밀번호·토큰·회원정보는 백업에 넣지 않습니다. 기본 제공 `sounds/`는 소스 ZIP에 보존되어 있으므로 음악 백업에는 중복으로 넣지 않습니다.

오디오만 복구할 때는 봇을 중지한 환경에서 선택적으로 다음 도구를 사용할 수 있습니다. 우선 검사만 하고, 확인 후 `--apply`를 붙입니다. 기존 다른 파일을 덮어쓰지 않으며 **회원·곡 DB/타순/설정 JSON을 자동 가져오는 도구가 아닙니다.** 기존 DB와 Volume을 그대로 유지하는 것이 우선입니다.

```sh
python scripts/restore_audio_backup.py 음악백업.zip --data-dir /data
python scripts/restore_audio_backup.py 음악백업.zip --data-dir /data --apply
```

## 10. 삼성·아이폰 앱 설치

웹의 로그인 화면과 상단 **앱 설치**를 누르면 `/download.html`이 열립니다. 동일한 웹 UI/API이므로 로그인 계정·권한·음악 데이터가 같습니다. 최초 앱 실행 때 로그인 세션은 다시 필요할 수 있습니다.

삼성/Android는 Chrome으로 열어 설치 버튼을 사용합니다. 실제 브라우저 설치 이벤트가 도착한 경우에만 버튼을 보여주며, 버튼이 없으면 브라우저의 앱 설치/홈 화면 추가 안내를 표시합니다. 이미 앱 모드로 실행 중인 화면에서는 설치 링크를 숨깁니다. 일반 브라우저 탭에서 이미 설치한 모든 경우를 완벽히 감지한다고 가정하지 않습니다.

iPhone은 Safari의 공유 → 홈 화면에 추가 → 웹 앱으로 열기(표시되는 경우) → 추가 순서입니다. 이것은 APK/IPA 다운로드가 아닙니다. 별도 스토어 심사/배포를 완료한 네이티브 앱도 아닙니다.

서비스 워커는 공개 오프라인 안내·스타일·아이콘만 캐시합니다. 로그인 응답/세션·API·개인 오디오·백업을 캐시하지 않고, 오프라인 제어 명령을 나중에 자동 실행하지 않습니다. 새 버전 적용 전 수정사항 저장을 안내합니다. 서버가 실행 중이고 인터넷이 연결되어야 제어할 수 있습니다.

공식 참고:
- https://support.apple.com/ko-kr/guide/iphone/iphea86e5236/ios
- https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/How_to/Trigger_install_prompt

## 11. 배포 후 직접 확인할 순서

관리자로 로그인해 필요한 계정 역할을 다시 지정합니다. OFF에서 ID 사용자가 입장해 자동곡이 나오지 않는지, ON에서 다시 입장할 때 나오는지 확인합니다. 이어서 스테이지 발언 권한/실제 청취, 등록/삭제자의 세 종류 곡 연결, 기본 상황 삭제·복원, 원본명 ZIP, 삼성·아이폰 실제 설치를 확인하세요.

오프라인 작업 환경에서 통과한 테스트와 확인할 수 없던 항목은 **TEST_REPORT_STUDIO.md**에 분리해 기록했습니다. 과거 버전의 테스트 보고서는 이번 버전 전체 검증 결과로 취급하지 마세요.
