services:
  appearance-bot:
    build: .
    env_file: .env
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      DATA_DIR: /data
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - bot-data:/data
    restart: unless-stopped
    stop_grace_period: 60s
volumes:
  bot-data:
