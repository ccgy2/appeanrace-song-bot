# 등장곡 · 응원가 · 상황별 노래 / 영구 저장 업데이트

기존 운영 방식은 바꾸지 않습니다.

```text
웹사이트: https://appearance-song.web.app
봇 API:  https://appeanrace-song-bot-production.up.railway.app
웹 파일 → 기존 웹사이트 폴더 → Firebase Hosting
봇 파일 → 기존 GitHub 저장소 → 기존 Railway 봇 서비스
```

이 파일은 배포용 수정본 설명입니다. 실제 Railway 계정의 볼륨을 만들거나 Firebase에 배포한 상태는 아닙니다.

## 먼저 알아둘 점 — 파일 보관에는 Railway 볼륨이 필요합니다

기존 코드는 **곡 정보**와 **실제 오디오**를 따로 저장합니다.

- `FIREBASE_SERVICE_ACCOUNT`가 있으면 곡/회원 정보는 기존 Firestore를 사용합니다.
- 직접 올린 MP3/WAV 등은 `DATA_DIR/uploads`에 저장합니다. Firestore에 곡 이름이 있다고 오디오까지 보관되는 것은 아닙니다.
- Firebase 키가 없으면 곡/회원 정보도 `DATA_DIR/songs.sqlite3`에 저장합니다.
- 브라우저를 닫거나 로그아웃하는 동작은 음악 파일을 삭제하지 않습니다.
- Railway의 볼륨 밖 파일은 배포 교체 이후 보존되지 않습니다. 같은 폴더 이름을 만들거나 `DATA_DIR=/data`만 설정하는 것은 볼륨 생성과 다릅니다.

### Railway에 한 번만 설정

**이미 볼륨을 쓰고 있으면 새로 만들거나 지우지 말고 그 볼륨과 경로를 유지하세요.**

새로 연결할 때는 Railway 프로젝트 화면에서 우클릭 → **Create Volume / Add Volume** → **지금 쓰는 봇 서비스**를 선택 → Mount Path를 `/data`로 지정합니다. 메뉴 위치에 따라 서비스의 Volumes에서 추가할 수도 있습니다.

같은 봇 서비스의 Variables에 다음을 설정하고 변경 사항을 배포합니다.

```dotenv
DATA_DIR=/data
```

기존 볼륨이 `/app/data`처럼 다른 경로를 쓰면 `DATA_DIR`도 그 기존 경로를 사용하세요. **이 안내 때문에 기존 볼륨을 지우거나 경로를 무작정 바꾸면 안 됩니다.**

`RAILWAY_VOLUME_MOUNT_PATH`는 볼륨 연결 시 Railway가 제공하는 값입니다. 직접 만들어서 속이지 마세요. 이번 코드는 실제 마운트도 확인합니다. `DATA_DIR`를 별도로 지정하지 않았다면 Railway가 제공하는 볼륨 경로를 자동으로 선택합니다. 명시한 `DATA_DIR`가 볼륨 밖이면 자동 이동하지 않고 경고합니다.

적용 후 웹 상단에 **영구 저장 연결됨**이 떠야 Railway에서 새 파일을 업로드할 수 있습니다. 볼륨이 없거나 저장 경로가 맞지 않으면 업로드를 차단합니다. Firebase도 쓰지 않는 Railway 환경이라면 곡·일반 계정 등의 로컬 변경도 차단하며, 기본 관리자는 로그인해서 진단을 볼 수 있습니다.

볼륨을 분리·삭제·초기화하거나 다른 서비스에 배포하면 파일 보관이 깨질 수 있습니다. 볼륨 용량과 요금은 Railway의 현재 요금제/사용량 조건을 확인하세요. 같은 봇은 한 서비스/한 인스턴스로 실행하세요.

### 아직 남아 있는 기존 파일은 변경 전에 백업

배포나 볼륨 추가/경로 변경 전에 남아 있는 원본 오디오와 기존 DB를 먼저 보관하세요. 이번 수정본을 적용한 뒤에는 **연결 진단 → 오디오 포함 음악 백업 (ZIP)**도 사용할 수 있습니다. 이전 버전에는 이 버튼이 없으므로 원본 파일을 PC에 보관하거나 기존 서버 파일을 별도로 백업해야 합니다.

이번 코드가 기존 컨테이너의 임시 파일을 자동으로 옮기지는 않습니다. 볼륨 연결로 기존 폴더가 가려지거나 이전 컨테이너가 교체되기 전에 별도 보관해야 합니다.

**이미 삭제된 오디오는 이번 수정본으로 자동 복구되지 않습니다.** 라이브러리에 곡 정보만 남아 있으면 삭제하지 말고 **수정 → 파일 업로드 → 원본 선택 → 저장**으로 연결하세요. 파일이 없어도 기존 항목은 감추지 않고 `파일 없음`으로 표시합니다. 파일을 바꾸지 않는 닉네임 수정은 원본이 없는 항목도 가능합니다.

## 1. 봇 수정본 적용

`appearance-bot-persistent-music-library.zip`을 풀고 내용물을 기존 봇 GitHub 저장소에 반영합니다. 새 저장소나 새 Railway 서비스를 만들 필요가 없습니다. ZIP 파일 자체가 아니라 내부 소스 파일들을 적용하세요.

추가 파일 `persistence.py`, `backups.py`도 반드시 넣습니다. `main.py`만 교체하지 마세요. Dockerfile 및 나머지 Python 모듈도 함께 업데이트합니다.

**기존 DISCORD_TOKEN, FIREBASE_SERVICE_ACCOUNT, OWNER_ID, WEB_ADMIN_PASSWORD와 기존 데이터/볼륨을 유지하세요.** 비밀키/실제 `.env`/업로드 DB를 GitHub에 올리지 않습니다. 계정은 새로 가입할 필요 없이 기존 DB의 `webUsers`를 이어서 사용합니다.

Railway 시작 명령은 기존과 같이 `python main.py`이고, 웹과 봇은 같은 프로세스에서 실행합니다. `WEB_ENABLED=true`, `WEB_ONLY=false`를 유지하세요.

## 2. 웹 수정본 적용

`appearance-website-music-library.zip` 안의 내용물을 다음 폴더에 덮어씁니다.

```text
G:\7. 디스코드 봇\appeanrace-song-bot-main\웹사이트
```

최종 구조:

```text
웹사이트\
├─ .firebaserc                  ← 기존 파일 유지; 이번 ZIP에는 없음
├─ firebase.json
├─ deploy-firebase.bat
├─ scripts\check-hosting.cjs
└─ static\
   ├─ index.html
   ├─ config.js
   ├─ app.js
   └─ style.css
```

CMD에서:

```bat
cd /d "G:\7. 디스코드 봇\appeanrace-song-bot-main\웹사이트"
firebase deploy --only hosting
```

`deploy-firebase.bat`를 실행해도 같은 작업을 합니다. `static` 안이나 압축을 푼 중첩 폴더 안에서 배포하지 말고, `firebase.json`이 있는 폴더에서 배포하세요. `firebase init`을 다시 실행할 필요는 없습니다.

**봇과 웹을 둘 다 업데이트해야 합니다.** 새 웹은 연결 확인 시 서버의 새 라이브러리 기능 지원도 검사합니다. 주소 입력 도구를 다시 실행하지 마세요. 실제 두 공개 주소는 그대로 들어 있습니다.

## 3. 세 종류를 각각 등록

웹 **음악 라이브러리**에서 위쪽의 **등장곡 / 응원가 / 상황별 노래**를 선택한 뒤 등록합니다. 각각 YouTube 링크 또는 직접 업로드를 사용할 수 있습니다.

| 종류 | 저장/재생 방식 |
|---|---|
| 등장곡 | 선수 닉네임과 사용자 ID 연결. ID가 있는 곡은 통화방 입장 자동 재생 대상으로 유지됩니다. |
| 응원가 | 별도 목록에 저장합니다. 같은 닉네임의 등장곡을 덮어쓰지 않습니다. 웹 재생 또는 `!응원가 이름`을 사용합니다. |
| 상황별 노래 | 홈런 축하곡 등 임의 이름으로 등록합니다. 웹 재생 또는 `!상황곡 이름`을 사용합니다. |

예를 들어 `김선수`의 등장곡과 `김선수`의 응원가를 각각 다른 파일로 등록할 수 있습니다. 자동 입장 재생은 **등장곡만** 사용합니다. 응원가·상황별 노래가 연달아 자동 재생되는 방식은 아닙니다.

새 파일을 올리면 등장곡은 처음 최대 30초, 응원가/상황별 노래는 전체 길이(최대 1시간)가 기본 구간으로 제안됩니다. 원하는 시작·끝 시간을 수정한 뒤 반드시 **저장**을 누르세요. 파일 업로드 완료만으로 라이브러리 항목이 생성되는 것은 아닙니다.

### 경기 사운드와 연결

관리자는 **경기 사운드 → 홈런/득점 등 선택 → 상황별 노래에서 선택 → 선택한 노래 연결**을 누를 수 있습니다. 연결한 곡의 재생 구간이 적용됩니다. 기존 기본 효과음, 기존 직접 교체한 효과음, `sounds/` 파일은 그대로 유지합니다.

상황별 노래 이름을 바꾸면 연결도 새 이름을 따라갑니다. 연결된 곡을 삭제하면 해당 연결을 해제하고 기본 효과음으로 돌아갑니다. 곡을 등록하기만 했다고 기본 효과음이 자동으로 교체되지는 않습니다.

## 4. 우리 팀 라이브러리에서 닉네임 변경

**해당 곡의 수정 → 선수 닉네임/곡 이름 변경 → 변경 내용 저장** 순서입니다.

새 곡을 하나 더 만드는 대신 기존 항목의 이름을 이동합니다. 등장곡을 바꾸면 그 이름을 참조하던 1~9번 타순도 같은 저장 작업에 함께 반영합니다. 오디오 ID, Discord 사용자 ID 연결, 설정한 재생 구간은 유지합니다. 같은 종류에 새 이름이 이미 있으면 덮어쓰지 않고 오류로 알려줍니다.

별도로 등록한 다른 종류의 동일 닉네임은 자동으로 바꾸지 않습니다. **Discord 서버 닉네임이나 웹 로그인 아이디를 바꾸는 기능도 아닙니다.** 사용자 ID가 없는 기존 닉네임 기반 자동 등장곡은 실제 Discord 닉네임과 일치해야 합니다.

## 5. 권한과 백업

기존 로그인/로그아웃/회원가입/관리자 승인은 유지합니다. 관리자와 승인된 재생자 모두 세 종류의 곡 등록·파일 업로드·수정·라이브러리 닉네임 변경을 할 수 있습니다. 삭제, 팀/타순 변경, 경기 사운드 연결 변경, 회원관리, 전체 음악 백업은 관리자 전용입니다. UI뿐 아니라 API에서도 검사합니다.

현재 구조의 재생자는 공유 라이브러리의 곡을 수정할 수 있습니다. 사용자별 곡 소유권이나 서버별 독립 라이브러리를 새로 도입한 버전은 아닙니다. 기존처럼 동일 팀 이름의 데이터는 봇 전체에서 공유됩니다.

로그아웃/서버 재시작 후 재로그인은 필요할 수 있지만, 음악 파일과 DB를 삭제하지는 않습니다. 회원의 권한 변경/사용 중지 시 해당 회원의 기존 웹 세션을 무효화합니다.

백업 종류:

- **설정 백업 (JSON)**: 세 종류의 곡 정보, 타순, 경기 사운드 연결 설정. 오디오 없음.
- **오디오 포함 음악 백업 (ZIP)**: 위 정보 + 현재 남아 있는 업로드 파일/파일 메타데이터. 없어진 파일은 `backup-info.json`의 `missingAssetIds`로 안내.
- **원본 다운로드**: 라이브러리의 업로드 항목에서 한 파일을 내려받음.

ZIP에는 계정/비밀번호, 봇 토큰/Firebase 키, 기본 `sounds/`는 넣지 않습니다. 자동 ZIP 가져오기 UI는 없습니다. 업로드 파일 복원 시 `uploads` 경로와 파일 ID를 보존하고, 기존 DB/볼륨에 덮어쓰기 전 반드시 기존 데이터도 백업해야 합니다. 볼륨 자체와 Firestore의 별도 백업도 유지하세요.

## 6. 확인 순서

배포 완료 → 웹에서 로그인 → **영구 저장 연결됨** 확인 → 테스트 파일 등록 및 저장 → 재생 → Railway 다시 시작 → 재로그인 → 같은 팀/종류에서 파일 재생 확인.

문제 발생 시 웹 상단 저장소 메시지와 연결 진단의 저장 경로를 먼저 확인하세요. 토큰, Firebase 키, 관리자 비밀번호는 대화/스크린샷에 노출하지 마세요.

## 공식 배포 기준

- Railway 볼륨/런타임 마운트 변수: https://docs.railway.com/volumes
- Railway 임시 파일시스템: https://docs.railway.com/services
- Railway 볼륨 제한/요금/백업: https://docs.railway.com/volumes/reference
- Firebase Hosting 배포: https://firebase.google.com/docs/hosting/quickstart

검증 범위와 한계는 `TEST_REPORT_LIBRARY.md`에 따로 기록했습니다.
