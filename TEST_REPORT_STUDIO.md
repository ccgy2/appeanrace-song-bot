# 2026-09-26 검증 결과 — studio-20260926-1

## 완료한 검사

`python -m unittest discover -s tests -v`: **143개 발견, 135개 통과, 8개 명시적 건너뜀, 실패/오류 0**.

새 `tests/test_studio_update.py`는 다음을 검사합니다. HTTP API에는 실제 로컬 aiohttp 서버와 임시 SQLite를 사용하고, 실제 FFprobe로 테스트 WAV를 검사했습니다. Discord/YouTube는 mock입니다.

- 재생자·유저의 등록/삭제/경기 사운드/회원/백업 API 직접 요청 차단
- 등록/삭제자의 업로드·세 종류 음악 연결·기본 상황 삭제 및 복원
- 역할 변경 시 이전 세션 무효화와 새 역할 적용
- 엄격한 ON/OFF 입력, CSRF, DB 저장, 두 Store 인스턴스 간 공유, 재로딩
- OFF에서 수동 재생은 허용하고 자동 준비/연결/재생은 차단
- 자동 준비 중 OFF→ON 전환 및 연결 중 OFF의 이전 재생 요청 취소
- 스테이지 발언자 재사용/권한에 따른 자동 전환/권한 없는 요청/청중 연결 유지/상태 확인 시간 초과
- 기본 상황 삭제가 새로고침 후에도 유지되고 다른 팀에는 영향을 주지 않음
- 같은 이름의 세 종류 곡 연결·변경·삭제 참조 구분과 예전 상황곡 연결 호환
- 잘못된 곡/파일/카테고리·최대 100곡 제한이 기존 설정을 손상하지 않음
- 한글 원본명, 동일 파일명 구분, 퍼센트 문자가 실제 이름에 있는 경우 보존
- 원본 오디오 바이트와 백업 매핑, 누락 오디오 감지, no-overwrite 오디오 복구 검사
- PWA 정적 경로, manifest MIME, 서비스 워커 허용 범위, 비공개 캐시 제외 규칙
- 스테이지 채널이 API 목록에 포함되고 세 종류 곡의 순서 재생이 길드별로 구분됨

`python tests/browser_studio_update.py`: **17개 시나리오 통과, 브라우저 JavaScript 오류 0**.

실제 Chromium에서 DOM 이벤트를 실행하되 API/XHR 업로드와 설치 이벤트는 명시적으로 만든 가짜 데이터입니다. 따라서 실시간 Discord/Firebase/Railway나 OS의 진짜 설치 성공을 입증하지 않습니다.

로그인, 새 역할별 화면, 자동 토글, 17개 기본/직접 추가 카드, 세 종류 곡 다중 선택, 실제 HTML 드래그 순서 변경, 여러 파일 선택, 파일 드롭, 변경사항 저장, 삭제/복원, 모바일 화살표, 관리자 역할 변경, 세션 만료 시 편집창 닫힘, 설치 이벤트 기반 버튼을 확인했습니다. 320·360·390·768·1500px에서 페이지/편집창 가로 넘침을 확인했습니다. 테스트 데이터로 촬영한 화면이며 사용자의 실제 곡을 표시한 캡처가 아닙니다.

구문/배포 검사:

```text
python -m compileall -q .                         통과
node --check static/app.js                       통과
node --check static/pwa.js                       통과
node --check static/sw.js                        통과
웹사이트/ 에서 node scripts/check-hosting.cjs      통과
```

원본 `sounds/` **52개 파일을 원본 ZIP과 SHA-256으로 비교해 바이트가 동일함을 확인**했습니다. 목록은 `tests/sounds-preservation-20260926.json`입니다. 원본 ZIP의 UTF-8 플래그 누락 이름은 UTF-8로 해석해 정상 경로로 추출했습니다.

## 구버전 테스트 갱신 / 건너뜀 사유

역할 변경·기본 상황의 복원 가능한 삭제·원본명 백업·빌드 번호에 관한 구버전 기대값은 새 명세에 맞게 갱신했습니다. 원래부터 다른 루트/Firebase `config.js`는 동일 바이트여야 한다는 잘못된 테스트 대신 각각의 연결 설정을 유지하는지 검사합니다.

`test_split`에 남아 있던 과거 `setup_deploy.py`용 테스트 8개는 원본 ZIP에 그 도구가 없으므로 명시적으로 건너뜁니다. 그 파일이 없어 전체 테스트 모듈을 읽지 못하던 문제를 정리하고, 같은 모듈의 CORS·세션·업로드·제어 HTTP 회귀 테스트는 실행했습니다. 실제 제공된 `웹사이트/firebase.json`과 predeploy 검사기는 별도로 통과했습니다.

과거 `browser_library_*.py` 등은 구버전 화면 전용 기록입니다. 이번 화면용 브라우저 테스트는 `browser_studio_update.py`입니다.

## 확인하지 못한 항목 — 통과로 계산하지 않음

1. 실제 Discord 토큰을 사용한 서버 접속, 실제 스테이지 권한/음성 청취, 두 봇의 실제 동시 재생. Discord/YouTube 의존성은 이 작업 환경에서 설치되어 있지 않았고 외부 패키지 다운로드 DNS가 제한되어 전체 `main.py` 런타임 부팅은 하지 못했습니다. 운영 의존성/Dockerfile은 기존 버전을 유지했습니다.
2. 실제 Railway/Firebase 배포, Volume/Firestore 운영 데이터에 대한 쓰기. 운영 데이터에 접근하거나 변경하지 않았습니다.
3. 삼성폰/아이폰의 실제 홈 화면 설치, Safari·Samsung Internet의 실기기 동작.
4. 진짜 서비스 워커의 오프라인 전환 통합 테스트. `browser_pwa_local.py`를 실행했지만 이 환경에서 브라우저의 localhost 탐색이 `net::ERR_BLOCKED_BY_ADMINISTRATOR`로 차단되어 페이지 탐색 단계에서 중단됐습니다. 차단을 우회하지 않았습니다. 테스트 코드는 일반 개발 환경에서 재실행할 수 있도록 제공합니다. API 서버의 정적 응답 검사는 별도로 통과했지만 실제 서비스 워커 설치/오프라인 복귀를 통과했다고 주장하지 않습니다.

결론: 파일 수정, 로컬 API/저장/권한/백업 검사와 분리된 브라우저 UI 검사는 완료했습니다. 실제 배포·Discord 청취·모바일 설치 확인은 운영 환경에서 남아 있습니다.
